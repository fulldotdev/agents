# Skills content backlog

Open content questions from the September 2026 wording cleanup. Work through these with the user after the wording and stripping rounds. Delete this file when it is empty.

- **Archive mail in triage.** Until 7 September 2026 (commit `6a53f44`) triage had: "archive a thread only when no reply, action, decision, payment, approval, clarification, or follow-up remains. An unsent draft stays in Inbox." Decide whether it comes back.
- **Local review in `development`.** The user wants QA for client delivery to lean much more on browser and computer use with snapshots, to be absolutely sure before handoff. The current section only describes a code review by a subagent.
- **A separate `browser` skill.** The Browser section may move out of `environment` into its own skill. Check what else in `environment` should move out.
- **Sales cron model.** The job has run on `opencode-go/glm-5.3-flash` since 16 September 2026, while the user earlier wanted it kept on Sol. Judge again after a few runs with the new template.
- **Alerts in triage.** Monitor warnings and security alerts without a record are no longer reported under the new template. Decide whether they get their own line type.
